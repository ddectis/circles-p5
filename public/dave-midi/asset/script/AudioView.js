/*
Currently not linked/used.
TODO: Load a soundfont or figure out how to adjust pitch of a known sample.
*/

export default class {

	onNotePressed(_value, _velocity) {

		//const context = new AudioContext();
	}
};